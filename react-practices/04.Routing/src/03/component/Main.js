import React from "react";

export default function Main() {
  return (
    <div>
      <h1>Main</h1>
      {/* <ul>
        <a
          href="/guestbook"
          onClick={(e) => {
            e.preventDefault();
            history.pushState({page: ~~}); // 브라우저랑 약속한 상태에 넣어줘야 브라우저가 이동을 안하고 주소창만 바뀜
          }}
        >
          방명록
        </a>
      </ul> */}
    </div>
  );
}
